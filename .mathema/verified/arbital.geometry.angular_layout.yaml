# machine record; binds to form 60e9062b5078
arbital.geometry.angular_layout:
  schema_version: "0.2.0"
  name: "angular_layout"
  signature: "(assoc: \"Mat('n', 'n')\", layout: \"Literal['spread', 'embed', 'ordered']\" = 'spread', min_gap_frac: 'float' = 0.35) -> 'np.ndarray'"
  intent: "Angle (radians) for each feature from the feature-feature matrix. assoc: symmetric matrix of r_info between features (diagonal 1).  We run classical MDS (Torgerson 1952) on the distance matrix d = 1 - assoc and read each feature's angle from the 2-D embedding, so mutually-associated features land near each other on the circle."
  grammar: "mathema"
  tolerance: 1.0e-09
  identity:
    form: "60e9062b5078"
    sig: "276160527b81"
    tier: 2
    source_available: true
    pure: true
    claims_fingerprint: "ba6b7821628c"
    pin: "none"
    integrity: "v2:62659ce81a2b291b"
  math: null
  claims:
    - name: "callable"
      statement: "f(assoc, layout, min_gap_frac) can be called"
      verdict: "skipped"
      note: "parameter 'layout' is a string with no declared domain; declare its values, e.g. 'for layout in {\"a\", \"b\"}, ...' in a claim, or annotate it Literal[...]"
      route: null
      authored:
        surface: "builtin"
      meta:
        mathema.probe_gap: "string-domain-missing"
        mathema.surface: "builtin"
    - name: "dependencies_current"
      verdict: "proven"
      note: "generated by mathema verify; adjudicated against the verified store"
      sketch: "every direct function dependency's form matches its verified record"
      route: "derive"
      authored:
        surface: "builtin"
      meta:
        mathema.surface: "builtin"
    - name: "is_arbitrary_input_safe"
      statement: "is_arbitrary_input_safe(layout)"
      verdict: "holds"
      n: 256
      note: "routes attempted, derive: unsupported (the registered family for this claim's own name couldn't decide it, and there is no ordinary derive route for this predicate); probe: holds"
      route: "probe:minimal_example"
      authored:
        surface: "claims-file"
        route: "best"
      meta:
        mathema.derive_status: "unsupported"
        mathema.surface: "declared"
  concepts:
    - "iteration"
  references: {}
  reasoning:
    - step: "intent"
      claim: "Angle (radians) for each feature from the feature-feature matrix. assoc: symmetric matrix of r_info between features (diagonal 1).  We run classical MDS (Torgerson 1952) on the distance matrix d = 1 - assoc and read each feature's angle from the 2-D embedding, so mutually-associated features land near each other on the circle."
      basis: "documented; the author's stated purpose"
    - step: "structure"
      claim: "comprehension pipeline: map"
      basis: "read off the AST"
    - step: "gap"
      claim: "f(assoc, layout, min_gap_frac) can be called"
      basis: "parameter 'layout' is a string with no declared domain; declare its values, e.g. 'for layout in {\"a\", \"b\"}, ...' in a claim, or annotate it Literal[...]"
    - step: "evidence"
      claim: "is_arbitrary_input_safe(layout)"
      basis: "probed, n=256"
    - step: "derivation"
      claim: ""
      basis: "every direct function dependency's form matches its verified record"
    - step: "situating"
      claim: "instantiates: iteration"
      basis: "deterministic concept tagging"
  lineage:
    generated_by: "mathema 0.6.0"
    CDD_spec_version: "0.2.0"
    date: "2026-09-25"
    commit: "52a40a030d8c9056657d5c8dfc6b9b8e57d0789e"
  dependencies:
    - name: "_classical_mds_2d"
      kind: "function"
      key: "arbital.geometry._classical_mds_2d"
      file: "src/arbital/geometry.py"
      line: 183
      form: "8fe9b7f2adef"
      freshness: "current"
    - name: "_min_gap"
      kind: "function"
      key: "arbital.geometry._min_gap"
      file: "src/arbital/geometry.py"
      line: 204
      form: "cd9fe7e2d3b7"
      freshness: "current"
    - name: "_widened_gap"
      kind: "function"
      key: "arbital.geometry._widened_gap"
      file: "src/arbital/geometry.py"
      line: 216
      form: "2805d75b8e7d"
      freshness: "current"
  raises:
    - "ValueError"
  meta:
    mathema.concept_sources:
      mechanism:
        - "iteration"
      keyword:
        - "metric-space"
    concepts:
      - "iteration"
    mathema.premise_state: {}
    mathema.intent_provenance: "declared"
