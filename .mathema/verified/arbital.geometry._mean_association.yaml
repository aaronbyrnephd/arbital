# machine record; binds to form 62fec79286e7
arbital.geometry._mean_association:
  name: "_mean_association"
  signature: "(row_sum: 'float', p: 'int') -> 'float'"
  intent: "Mean association of one column to the other p - 1 columns. `row_sum` is that column's row of the association matrix summed, including its own diagonal 1, which is subtracted off here."
  grammar: "mathema"
  identity:
    form: "62fec79286e7"
    sig: "d9fc46628283"
    tier: 2
    pure: true
    claims_fingerprint: "820590f8dd78"
    integrity: "ce4bb65e2ef4cac1"
  math: null
  claims:
    - name: "nothing_associated_scores_zero"
      statement: "for p in [2, 50]:int|missing, f(1, p) = 0"
      verdict: "proven"
      n: null
      counterexample: null
      note: "; inferred row_sum=1 from the claim's own literal argument"
      sketch: "0 and 0 simplify identically"
      condition: "for p in [2, 50]:int|missing"
      route: "derive"
      authored: "declared"
      domain:
        p:
          base_type: "Z"
          pieces:
            - lo: 2.0
              hi: 50.0
              closed_lo: true
              closed_hi: true
          excluded: []
          explicit_type: true
      grammar: "mathema"
      tolerance: null
      meta:
        mathema.claim_source: "the author"
    - name: "everything_associated_scores_one"
      statement: "for p in [2, 50]:int|missing, f(p, p) = 1"
      verdict: "proven"
      n: null
      counterexample: null
      note: ""
      sketch: "1 and 1 simplify identically"
      condition: "for p in [2, 50]:int|missing"
      route: "derive"
      authored: "declared"
      domain:
        p:
          base_type: "Z"
          pieces:
            - lo: 2.0
              hi: 50.0
              closed_lo: true
              closed_hi: true
          excluded: []
          explicit_type: true
      grammar: "mathema"
      tolerance: null
      meta:
        mathema.claim_source: "the author"
    - name: "stays_on_the_association_scale"
      statement: "assuming row_sum <= p, for p in [2, 50]:int|missing, row_sum in [1.0, 50.0]:float|missing, 0 <= f(row_sum, p) <= 1"
      verdict: "holds"
      n: 69
      counterexample: null
      note: "every link of the chained comparison holds"
      sketch: null
      condition: "for p in [2, 50]:int|missing, row_sum in [1.0, 50.0]:float|missing"
      route: "probe"
      authored: "declared"
      domain:
        p:
          base_type: "Z"
          pieces:
            - lo: 2.0
              hi: 50.0
              closed_lo: true
              closed_hi: true
          excluded: []
          explicit_type: true
        row_sum:
          lo: 1.0
          hi: 50.0
          closed_lo: true
          closed_hi: true
      grammar: "mathema"
      tolerance: null
      meta:
        mathema.claim_source: "the author"
    - name: "rises_with_the_row_sum"
      statement: "for p in [2, 50]:int|missing, row_sum in [1.0, 50.0]:float|missing, d(f(row_sum, p), row_sum) >= 0"
      verdict: "proven"
      n: null
      counterexample: null
      note: ""
      sketch: "interval evaluation over the declared domain: 1/(p - 1) ∈ AccumBounds(1/49, 1), never negative"
      condition: "∀ p ∈ [2, 50] ⊂ ℤ ∪ {∅}"
      route: "derive"
      authored: "declared"
      domain:
        p:
          base_type: "Z"
          pieces:
            - lo: 2.0
              hi: 50.0
              closed_lo: true
              closed_hi: true
          excluded: []
          explicit_type: true
        row_sum:
          lo: 1.0
          hi: 50.0
          closed_lo: true
          closed_hi: true
      grammar: "mathema"
      tolerance: null
      meta:
        mathema.claim_source: "the author"
    - name: "dependencies_current"
      statement: "dependencies_current"
      verdict: "proven"
      n: null
      counterexample: null
      note: "generated by mathema verify; adjudicated against the verified store"
      sketch: "no direct function dependencies, vacuously current"
      condition: null
      route: "derive"
      authored: "builtin"
      domain: null
      grammar: null
      tolerance: null
      meta:
        mathema.claim_source: "builtin"
  concepts:
    - "differentiation"
  references: {}
  reasoning:
    - step: "intent"
      claim: "Mean association of one column to the other p - 1 columns. `row_sum` is that column's row of the association matrix summed, including its own diagonal 1, which is subtracted off here."
      basis: "documented; the author's stated purpose"
    - step: "derivation"
      claim: "for p in [2, 50]:int|missing, f(1, p) = 0"
      basis: "0 and 0 simplify identically"
    - step: "derivation"
      claim: "for p in [2, 50]:int|missing, f(p, p) = 1"
      basis: "1 and 1 simplify identically"
    - step: "evidence"
      claim: "assuming row_sum <= p, for p in [2, 50]:int|missing, row_sum in [1.0, 50.0]:float|missing, 0 <= f(row_sum, p) <= 1"
      basis: "probed, n=69"
    - step: "derivation"
      claim: "for p in [2, 50]:int|missing, row_sum in [1.0, 50.0]:float|missing, d(f(row_sum, p), row_sum) >= 0"
      basis: "interval evaluation over the declared domain: 1/(p - 1) ∈ AccumBounds(1/49, 1), never negative"
    - step: "derivation"
      claim: "dependencies_current"
      basis: "no direct function dependencies, vacuously current"
    - step: "situating"
      claim: "instantiates: differentiation"
      basis: "deterministic concept tagging"
  lineage:
    generated_by: "mathema 0.6.0"
    CDD_spec_version: "0.1.0"
    date: "2026-09-14"
    commit: "da5bffc046752b1f53188550392524b4f6aff063"
  meta:
    mathema.concept_sources:
      mechanism:
        - "differentiation"
      keyword:
        - "weighted-average"
    concepts:
      - "differentiation"
    mathema.intent_provenance: "declared"
