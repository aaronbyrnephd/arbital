# machine record; binds to form 2805d75b8e7d
arbital.geometry._widened_gap:
  schema_version: "0.2.0"
  name: "_widened_gap"
  signature: "(gap: 'float', g_min: 'float', scale: 'float') -> 'float'"
  intent: "One angular gap after the \"spread\" rescaling. Every gap is raised to at least g_min, and whatever it had above g_min is kept in proportion `scale`, so the gaps that were large stay large relative to each other while the pile-ups are opened."
  grammar: "mathema"
  tolerance: 1.0e-09
  identity:
    form: "2805d75b8e7d"
    sig: "90612bd33cd2"
    tier: 2
    source_available: true
    pure: true
    claims_fingerprint: "3c3da84ced87"
    pin: "none"
    integrity: "v2:fa844f0b42ad3e6f"
  math:
    unicode: "g_min + scale*max(0, -g_min + gap)"
    latex: "g_{min} + scale \\max\\left(0, - g_{min} + gap\\right)"
    srepr: "Add(Symbol('g_min', real=True), Mul(Symbol('scale', real=True), Max(Integer(0), Add(Mul(Integer(-1), Symbol('g_min', real=True)), Symbol('gap', real=True)))))"
  claims:
    - name: "a_gap_already_at_the_minimum_is_unchanged"
      statement: "for g_min in [0.0, 1.0]:float|missing, scale in [0.0, 1.0]:float|missing, f(g_min, g_min, scale) = g_min"
      verdict: "proven"
      sketch: "g_min and g_min simplify identically"
      condition: "where x=g_min: ∀ x ∈ [0.0, 1.0] ⊂ ℝ ∪ {∅}"
      route: "derive"
      authored:
        surface: "claims-file"
        route: "best"
      domain:
        g_min:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
        scale:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
      meta:
        mathema.float_companion: "a_gap_already_at_the_minimum_is_unchanged[float]"
        mathema.surface: "declared"
    - name: "a_gap_already_at_the_minimum_is_unchanged[float]"
      statement: "for g_min in [0.0, 1.0]:float|missing, scale in [0.0, 1.0]:float|missing, f(g_min, g_min, scale) = g_min"
      verdict: "holds"
      n: 48
      note: "the implementation of a_gap_already_at_the_minimum_is_unchanged, executed in float at 48 points (every domain corner, then sampled interior points); unbounded directions (gap) run to magnitude 1e+308, sampled log-uniformly (no |inf| declared)"
      route: "probe"
      authored:
        surface: "claims-file"
      domain:
        g_min:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
        scale:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
      meta:
        mathema.surface: "declared"
        mathema.companion_of: "a_gap_already_at_the_minimum_is_unchanged"
        mathema.family: "is_numerically_stable"
    - name: "dependencies_current"
      verdict: "proven"
      note: "generated by mathema verify; adjudicated against the verified store"
      sketch: "no direct function dependencies, vacuously current"
      route: "derive"
      authored:
        surface: "builtin"
      meta:
        mathema.surface: "builtin"
    - name: "keeps_the_order_of_the_gaps"
      statement: "for gap in [0.0, 7.0]:float|missing, g_min in [0.0, 1.0]:float|missing, scale in [0.0, 1.0]:float|missing, d(f(gap, g_min, scale), gap) >= 0"
      verdict: "holds"
      n: 60
      note: "numeric evidence on the LIFTED intermediate (60 samples of the resolved symbolic form, mathema's reconstruction, not the code; the symbolic comparison itself stayed undecided); routes attempted, derive: undecided (sympy could not settle the sign of scale*Heaviside(-g_min + gap); extensive attempts did not settle it either: exact real-root isolation, interval refinement, widened wall-clock retry); probe: holds"
      route: "probe:lifted_numeric"
      authored:
        surface: "claims-file"
        route: "best"
      domain:
        gap:
          lo: 0.0
          hi: 7.0
          closed_lo: true
          closed_hi: true
        g_min:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
        scale:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
      meta:
        mathema.derive_status: "undecided"
        mathema.lifted_numeric_samples: 60
        mathema.surface: "declared"
    - name: "never_below_the_minimum"
      statement: "for gap in [0.0, 7.0]:float|missing, g_min in [0.0, 1.0]:float|missing, scale in [0.0, 1.0]:float|missing, f(gap, g_min, scale) >= g_min"
      verdict: "proven"
      sketch: "interval evaluation over the declared domain: scale*Max(0, -g_min + gap) ∈ AccumBounds(0, 7), never negative"
      condition: "where x=gap, y=g_min, z=scale: ∀ x ∈ [0.0, 7.0] ⊂ ℝ ∪ {∅}, y, z ∈ [0.0, 1.0] ⊂ ℝ ∪ {∅}"
      route: "derive"
      authored:
        surface: "claims-file"
        route: "best"
      domain:
        gap:
          lo: 0.0
          hi: 7.0
          closed_lo: true
          closed_hi: true
        g_min:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
        scale:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
      meta:
        mathema.float_companion: "never_below_the_minimum[float]"
        mathema.surface: "declared"
    - name: "never_below_the_minimum[float]"
      statement: "for gap in [0.0, 7.0]:float|missing, g_min in [0.0, 1.0]:float|missing, scale in [0.0, 1.0]:float|missing, f(gap, g_min, scale) >= g_min"
      verdict: "holds"
      n: 48
      note: "the implementation of never_below_the_minimum, executed in float at 48 points (every domain corner, then sampled interior points)"
      route: "probe"
      authored:
        surface: "claims-file"
      domain:
        gap:
          lo: 0.0
          hi: 7.0
          closed_lo: true
          closed_hi: true
        g_min:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
        scale:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
      meta:
        mathema.surface: "declared"
        mathema.companion_of: "never_below_the_minimum"
        mathema.family: "is_numerically_stable"
  concepts:
    - "differentiation"
  references: {}
  reasoning:
    - step: "intent"
      claim: "One angular gap after the \"spread\" rescaling. Every gap is raised to at least g_min, and whatever it had above g_min is kept in proportion `scale`, so the gaps that were large stay large relative to each other while the pile-ups are opened."
      basis: "documented; the author's stated purpose"
    - step: "formalization"
      claim: "g_min + scale*max(0, -g_min + gap)"
      basis: "symbolic lift of the body"
    - step: "derivation"
      claim: "for gap in [0.0, 7.0]:float|missing, g_min in [0.0, 1.0]:float|missing, scale in [0.0, 1.0]:float|missing, f(gap, g_min, scale) >= g_min"
      basis: "interval evaluation over the declared domain: scale*Max(0, -g_min + gap) ∈ AccumBounds(0, 7), never negative"
    - step: "evidence"
      claim: "for gap in [0.0, 7.0]:float|missing, g_min in [0.0, 1.0]:float|missing, scale in [0.0, 1.0]:float|missing, f(gap, g_min, scale) >= g_min"
      basis: "probed, n=48"
    - step: "evidence"
      claim: "for gap in [0.0, 7.0]:float|missing, g_min in [0.0, 1.0]:float|missing, scale in [0.0, 1.0]:float|missing, d(f(gap, g_min, scale), gap) >= 0"
      basis: "probed, n=60"
    - step: "derivation"
      claim: "for g_min in [0.0, 1.0]:float|missing, scale in [0.0, 1.0]:float|missing, f(g_min, g_min, scale) = g_min"
      basis: "g_min and g_min simplify identically"
    - step: "evidence"
      claim: "for g_min in [0.0, 1.0]:float|missing, scale in [0.0, 1.0]:float|missing, f(g_min, g_min, scale) = g_min"
      basis: "probed, n=48"
    - step: "derivation"
      claim: ""
      basis: "no direct function dependencies, vacuously current"
    - step: "situating"
      claim: "instantiates: differentiation"
      basis: "deterministic concept tagging"
  lineage:
    generated_by: "mathema 0.6.0"
    CDD_spec_version: "0.2.0"
    date: "2026-09-25"
    commit: "52a40a030d8c9056657d5c8dfc6b9b8e57d0789e"
  meta:
    mathema.concept_sources:
      mechanism:
        - "differentiation"
    concepts:
      - "differentiation"
    mathema.premise_state: {}
    mathema.intent_provenance: "declared"
