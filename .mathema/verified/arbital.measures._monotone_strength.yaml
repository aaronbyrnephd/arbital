# machine record; binds to form 13c9c97badda
arbital.measures._monotone_strength:
  schema_version: "0.2.0"
  name: "_monotone_strength"
  signature: "(r: 'float', rho: 'float') -> 'float'"
  intent: "Best monotone description of a pair: r_mono = max(|r|, |rho|)."
  grammar: "mathema"
  tolerance: 1.0e-09
  identity:
    form: "13c9c97badda"
    sig: "a98c3a736c49"
    tier: 2
    source_available: true
    pure: true
    claims_fingerprint: "4dc4003c1f31"
    pin: "none"
    integrity: "v2:6a93e6909e64c666"
  math:
    unicode: "max(abs(r), abs(rho))"
    latex: "\\max\\left(\\left|{r}\\right|, \\left|{\\rho}\\right|\\right)"
    srepr: "Max(Abs(Symbol('r', real=True)), Abs(Symbol('rho', real=True)))"
  claims:
    - name: "blind_to_the_pearson_sign"
      statement: "f(-r, rho) = f(r, rho)"
      verdict: "proven"
      sketch: "Max(Abs(r), Abs(rho)) and Max(Abs(r), Abs(rho)) simplify identically"
      condition: "where x=rho: ∀ r, x ∈ ℝ"
      route: "derive"
      authored:
        surface: "claims-file"
        route: "best"
      meta:
        mathema.float_companion: "blind_to_the_pearson_sign[float]"
        mathema.surface: "declared"
    - name: "blind_to_the_pearson_sign[float]"
      statement: "f(-r, rho) = f(r, rho)"
      verdict: "holds"
      n: 44
      note: "the implementation of blind_to_the_pearson_sign, executed in float at 44 points (every domain corner, then sampled interior points); unbounded directions (r, rho) run to magnitude 1e+308, sampled log-uniformly (no |inf| declared)"
      route: "probe"
      authored:
        surface: "claims-file"
      meta:
        mathema.surface: "declared"
        mathema.companion_of: "blind_to_the_pearson_sign"
        mathema.family: "is_numerically_stable"
    - name: "blind_to_the_spearman_sign"
      statement: "f(r, -rho) = f(r, rho)"
      verdict: "proven"
      sketch: "Max(Abs(r), Abs(rho)) and Max(Abs(r), Abs(rho)) simplify identically"
      condition: "where x=rho: ∀ r, x ∈ ℝ"
      route: "derive"
      authored:
        surface: "claims-file"
        route: "best"
      meta:
        mathema.float_companion: "blind_to_the_spearman_sign[float]"
        mathema.surface: "declared"
    - name: "blind_to_the_spearman_sign[float]"
      statement: "f(r, -rho) = f(r, rho)"
      verdict: "holds"
      n: 44
      note: "the implementation of blind_to_the_spearman_sign, executed in float at 44 points (every domain corner, then sampled interior points); unbounded directions (r, rho) run to magnitude 1e+308, sampled log-uniformly (no |inf| declared)"
      route: "probe"
      authored:
        surface: "claims-file"
      meta:
        mathema.surface: "declared"
        mathema.companion_of: "blind_to_the_spearman_sign"
        mathema.family: "is_numerically_stable"
    - name: "bounded_on_correlation_inputs"
      statement: "for r in [-1.0, 1.0]:float|missing, rho in [-1.0, 1.0]:float|missing, 0 <= f(r, rho) <= 1"
      verdict: "proven"
      note: "every link of the chained comparison proven"
      condition: "for r in [-1.0, 1.0]:float|missing, rho in [-1.0, 1.0]:float|missing"
      route: "derive"
      authored:
        surface: "claims-file"
        route: "best"
      domain:
        r:
          lo: -1.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
        rho:
          lo: -1.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
      meta:
        mathema.float_companion: "bounded_on_correlation_inputs[float]"
        mathema.surface: "declared"
    - name: "bounded_on_correlation_inputs[float]"
      statement: "for r in [-1.0, 1.0]:float|missing, rho in [-1.0, 1.0]:float|missing, 0 <= f(r, rho) <= 1"
      verdict: "holds"
      n: 44
      note: "the implementation of bounded_on_correlation_inputs, executed in float link by link; every link of the float companion holds"
      route: "probe"
      authored:
        surface: "claims-file"
      domain:
        r:
          lo: -1.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
        rho:
          lo: -1.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
      meta:
        mathema.surface: "declared"
        mathema.companion_of: "bounded_on_correlation_inputs"
        mathema.family: "is_numerically_stable"
    - name: "dependencies_current"
      verdict: "proven"
      note: "generated by mathema verify; adjudicated against the verified store"
      sketch: "no direct function dependencies, vacuously current"
      route: "derive"
      authored:
        surface: "builtin"
      meta:
        mathema.surface: "builtin"
    - name: "dominates_pearson"
      statement: "f(r, rho) >= |r|"
      verdict: "proven"
      sketch: "interval evaluation over the declared domain: -Abs(r) + Max(Abs(r), Abs(rho)) ∈ 0, never negative"
      condition: "where x=rho: ∀ r, x ∈ ℝ"
      route: "derive"
      authored:
        surface: "claims-file"
        route: "best"
      meta:
        mathema.float_companion: "dominates_pearson[float]"
        mathema.surface: "declared"
    - name: "dominates_pearson[float]"
      statement: "f(r, rho) >= |r|"
      verdict: "holds"
      n: 44
      note: "the implementation of dominates_pearson, executed in float at 44 points (every domain corner, then sampled interior points); unbounded directions (r, rho) run to magnitude 1e+308, sampled log-uniformly (no |inf| declared)"
      route: "probe"
      authored:
        surface: "claims-file"
      meta:
        mathema.surface: "declared"
        mathema.companion_of: "dominates_pearson"
        mathema.family: "is_numerically_stable"
    - name: "dominates_spearman"
      statement: "f(r, rho) >= |rho|"
      verdict: "proven"
      sketch: "interval evaluation over the declared domain: -Abs(rho) + Max(Abs(r), Abs(rho)) ∈ 0, never negative"
      condition: "where x=rho: ∀ r, x ∈ ℝ"
      route: "derive"
      authored:
        surface: "claims-file"
        route: "best"
      meta:
        mathema.float_companion: "dominates_spearman[float]"
        mathema.surface: "declared"
    - name: "dominates_spearman[float]"
      statement: "f(r, rho) >= |rho|"
      verdict: "holds"
      n: 44
      note: "the implementation of dominates_spearman, executed in float at 44 points (every domain corner, then sampled interior points); unbounded directions (r, rho) run to magnitude 1e+308, sampled log-uniformly (no |inf| declared)"
      route: "probe"
      authored:
        surface: "claims-file"
      meta:
        mathema.surface: "declared"
        mathema.companion_of: "dominates_spearman"
        mathema.family: "is_numerically_stable"
    - name: "stays_on_the_correlation_scale"
      statement: "for r in [-1.0, 1.0]:float|missing, rho in [-1.0, 1.0]:float|missing, 0 <= f(r, rho) <= 1"
      verdict: "proven"
      note: "every link of the chained comparison proven"
      condition: "for r in [-1.0, 1.0]:float|missing, rho in [-1.0, 1.0]:float|missing"
      route: "derive"
      authored:
        surface: "claims-file"
        route: "best"
      domain:
        r:
          lo: -1.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
        rho:
          lo: -1.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
      meta:
        mathema.float_companion: "stays_on_the_correlation_scale[float]"
        mathema.surface: "declared"
    - name: "stays_on_the_correlation_scale[float]"
      statement: "for r in [-1.0, 1.0]:float|missing, rho in [-1.0, 1.0]:float|missing, 0 <= f(r, rho) <= 1"
      verdict: "holds"
      n: 44
      note: "the implementation of stays_on_the_correlation_scale, executed in float link by link; every link of the float companion holds"
      route: "probe"
      authored:
        surface: "claims-file"
      domain:
        r:
          lo: -1.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
        rho:
          lo: -1.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
      meta:
        mathema.surface: "declared"
        mathema.companion_of: "stays_on_the_correlation_scale"
        mathema.family: "is_numerically_stable"
    - name: "symmetric_in_its_two_channels"
      statement: "f(r, rho) = f(rho, r)"
      verdict: "proven"
      sketch: "Max(Abs(r), Abs(rho)) and Max(Abs(r), Abs(rho)) simplify identically"
      condition: "where x=rho: ∀ r, x ∈ ℝ"
      route: "derive"
      authored:
        surface: "claims-file"
        route: "best"
      meta:
        mathema.float_companion: "symmetric_in_its_two_channels[float]"
        mathema.surface: "declared"
    - name: "symmetric_in_its_two_channels[float]"
      statement: "f(r, rho) = f(rho, r)"
      verdict: "holds"
      n: 44
      note: "the implementation of symmetric_in_its_two_channels, executed in float at 44 points (every domain corner, then sampled interior points); unbounded directions (r, rho) run to magnitude 1e+308, sampled log-uniformly (no |inf| declared)"
      route: "probe"
      authored:
        surface: "claims-file"
      meta:
        mathema.surface: "declared"
        mathema.companion_of: "symmetric_in_its_two_channels"
        mathema.family: "is_numerically_stable"
  concepts: []
  references: {}
  reasoning:
    - step: "intent"
      claim: "Best monotone description of a pair: r_mono = max(|r|, |rho|)."
      basis: "documented; the author's stated purpose"
    - step: "formalization"
      claim: "max(abs(r), abs(rho))"
      basis: "symbolic lift of the body"
    - step: "derivation"
      claim: "f(-r, rho) = f(r, rho)"
      basis: "Max(Abs(r), Abs(rho)) and Max(Abs(r), Abs(rho)) simplify identically"
    - step: "evidence"
      claim: "f(-r, rho) = f(r, rho)"
      basis: "probed, n=44"
    - step: "derivation"
      claim: "f(r, -rho) = f(r, rho)"
      basis: "Max(Abs(r), Abs(rho)) and Max(Abs(r), Abs(rho)) simplify identically"
    - step: "evidence"
      claim: "f(r, -rho) = f(r, rho)"
      basis: "probed, n=44"
    - step: "derivation"
      claim: "f(r, rho) = f(rho, r)"
      basis: "Max(Abs(r), Abs(rho)) and Max(Abs(r), Abs(rho)) simplify identically"
    - step: "evidence"
      claim: "f(r, rho) = f(rho, r)"
      basis: "probed, n=44"
    - step: "derivation"
      claim: "f(r, rho) >= |r|"
      basis: "interval evaluation over the declared domain: -Abs(r) + Max(Abs(r), Abs(rho)) ∈ 0, never negative"
    - step: "evidence"
      claim: "f(r, rho) >= |r|"
      basis: "probed, n=44"
    - step: "derivation"
      claim: "f(r, rho) >= |rho|"
      basis: "interval evaluation over the declared domain: -Abs(rho) + Max(Abs(r), Abs(rho)) ∈ 0, never negative"
    - step: "evidence"
      claim: "f(r, rho) >= |rho|"
      basis: "probed, n=44"
    - step: "derivation"
      claim: "for r in [-1.0, 1.0]:float|missing, rho in [-1.0, 1.0]:float|missing, 0 <= f(r, rho) <= 1"
      basis: "symbolic derivation"
    - step: "evidence"
      claim: "for r in [-1.0, 1.0]:float|missing, rho in [-1.0, 1.0]:float|missing, 0 <= f(r, rho) <= 1"
      basis: "probed, n=44"
    - step: "derivation"
      claim: "for r in [-1.0, 1.0]:float|missing, rho in [-1.0, 1.0]:float|missing, 0 <= f(r, rho) <= 1"
      basis: "symbolic derivation"
    - step: "evidence"
      claim: "for r in [-1.0, 1.0]:float|missing, rho in [-1.0, 1.0]:float|missing, 0 <= f(r, rho) <= 1"
      basis: "probed, n=44"
    - step: "derivation"
      claim: ""
      basis: "no direct function dependencies, vacuously current"
  lineage:
    generated_by: "mathema 0.6.0"
    CDD_spec_version: "0.2.0"
    date: "2026-09-25"
    commit: "52a40a030d8c9056657d5c8dfc6b9b8e57d0789e"
  meta:
    mathema.premise_state: {}
    mathema.intent_provenance: "declared"
