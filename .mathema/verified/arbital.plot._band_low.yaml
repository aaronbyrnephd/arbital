# machine record; binds to form 621b1cda77d0
arbital.plot._band_low:
  schema_version: "0.2.0"
  name: "_band_low"
  signature: "(r_info: 'float', se: 'float') -> 'float'"
  intent: "Outer end of the uncertainty band, in r_info units."
  grammar: "mathema"
  tolerance: 1.0e-09
  identity:
    form: "621b1cda77d0"
    sig: "8fc5aa68e14a"
    tier: 2
    source_available: true
    pure: true
    claims_fingerprint: "b01d5acfd0af"
    pin: "none"
    integrity: "v2:3848c6b657c61778"
  math:
    unicode: "min(999/1000, max(0, r_info - se))"
    latex: "\\min\\left(\\frac{999}{1000}, \\max\\left(0, r_{info} - se\\right)\\right)"
    srepr: "Min(Rational(999, 1000), Max(Integer(0), Add(Symbol('r_info', real=True), Mul(Integer(-1), Symbol('se', real=True)))))"
  claims:
    - name: "dependencies_current"
      verdict: "proven"
      note: "generated by mathema verify; adjudicated against the verified store"
      sketch: "no direct function dependencies, vacuously current"
      route: "derive"
      authored:
        surface: "builtin"
      meta:
        mathema.surface: "builtin"
    - name: "never_above_the_estimate"
      statement: "for r_info in [0.0, 1.0]:float|missing, se in [0.0, 1.0]:float|missing, f(r_info, se) <= r_info"
      verdict: "holds"
      n: 132
      note: "routes attempted, derive: undecided (sympy could not settle the sign of r_info - Min(999/1000, Max(0, r_info - se)); extensive attempts did not settle it either: exact real-root isolation, interval refinement, widened wall-clock retry); probe: holds"
      route: "probe"
      authored:
        surface: "claims-file"
        route: "best"
      domain:
        r_info:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
        se:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
      meta:
        mathema.sampling: "r_info~U(0,1)⊔{lo,hi,mid,±ε}[p=.3], se~U(0,1)⊔{lo,hi,mid,±ε}[p=.3], seed=20260718, n=132"
        mathema.confidence:
          score: 10.0
          stars: 4
          max_stars: 5
          n: 132
          factors:
            branches: 0
            loops: 0
            params: 0
            wide_domain: 0
            float_extremes: 0
        mathema.derive_status: "undecided"
        mathema.surface: "declared"
    - name: "never_negative"
      statement: "for r_info in [0.0, 1.0]:float|missing, se in [0.0, 1.0]:float|missing, f(r_info, se) >= 0"
      verdict: "proven"
      sketch: "interval evaluation over the declared domain: Min(999/1000, Max(0, r_info - se)) ∈ AccumBounds(0, 999/1000), never negative"
      condition: "where x=r_info, y=se: ∀ x, y ∈ [0.0, 1.0] ⊂ ℝ ∪ {∅}"
      route: "derive"
      authored:
        surface: "claims-file"
        route: "best"
      domain:
        r_info:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
        se:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
      meta:
        mathema.float_companion: "never_negative[float]"
        mathema.surface: "declared"
    - name: "never_negative[float]"
      statement: "for r_info in [0.0, 1.0]:float|missing, se in [0.0, 1.0]:float|missing, f(r_info, se) >= 0"
      verdict: "holds"
      n: 44
      note: "the implementation of never_negative, executed in float at 44 points (every domain corner, then sampled interior points)"
      route: "probe"
      authored:
        surface: "claims-file"
      domain:
        r_info:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
        se:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
      meta:
        mathema.surface: "declared"
        mathema.companion_of: "never_negative"
        mathema.family: "is_numerically_stable"
    - name: "the_band_is_ordered"
      statement: "let g = arbital.plot._band_high, for r_info in [0.0, 1.0]:float|missing, se in [0.0, 1.0]:float|missing, f(r_info, se) <= g(r_info, se)"
      verdict: "holds"
      n: 132
      note: "routes attempted, derive: undecided (sympy could not settle the sign of Min(999/1000, r_info + se) - Min(999/1000, Max(0, r_info - se)); extensive attempts did not settle it either: exact real-root isolation, interval refinement, widened wall-clock retry); probe: holds"
      route: "probe"
      authored:
        surface: "claims-file"
        route: "best"
      domain:
        r_info:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
        se:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
      meta:
        mathema.sampling: "r_info~U(0,1)⊔{lo,hi,mid,±ε}[p=.3], se~U(0,1)⊔{lo,hi,mid,±ε}[p=.3], seed=20260718, n=132"
        mathema.confidence:
          score: 10.0
          stars: 4
          max_stars: 5
          n: 132
          factors:
            branches: 0
            loops: 0
            params: 0
            wide_domain: 0
            float_extremes: 0
        mathema.derive_status: "undecided"
        mathema.surface: "declared"
    - name: "the_band_is_ordered_below_the_cap"
      statement: "let g = arbital.plot._band_high, for r_info in [0.0, 0.999]:float|missing, se in [0.0, 1.0]:float|missing, f(r_info, se) <= g(r_info, se)"
      verdict: "holds"
      n: 132
      note: "routes attempted, derive: undecided (sympy could not settle the sign of -Max(0, r_info - se) + Min(999/1000, r_info + se); extensive attempts did not settle it either: exact real-root isolation, interval refinement, widened wall-clock retry); probe: holds"
      route: "probe"
      authored:
        surface: "claims-file"
        route: "best"
      domain:
        r_info:
          lo: 0.0
          hi: 0.999
          closed_lo: true
          closed_hi: true
        se:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
      meta:
        mathema.sampling: "r_info~U(0,0.999)⊔{lo,hi,mid,±ε}[p=.3], se~U(0,1)⊔{lo,hi,mid,±ε}[p=.3], seed=20260718, n=132"
        mathema.confidence:
          score: 10.0
          stars: 4
          max_stars: 5
          n: 132
          factors:
            branches: 0
            loops: 0
            params: 0
            wide_domain: 0
            float_extremes: 0
        mathema.derive_status: "undecided"
        mathema.surface: "declared"
  concepts: []
  references: {}
  reasoning:
    - step: "intent"
      claim: "Outer end of the uncertainty band, in r_info units."
      basis: "documented; the author's stated purpose"
    - step: "formalization"
      claim: "min(999/1000, max(0, r_info - se))"
      basis: "symbolic lift of the body"
    - step: "derivation"
      claim: "for r_info in [0.0, 1.0]:float|missing, se in [0.0, 1.0]:float|missing, f(r_info, se) >= 0"
      basis: "interval evaluation over the declared domain: Min(999/1000, Max(0, r_info - se)) ∈ AccumBounds(0, 999/1000), never negative"
    - step: "evidence"
      claim: "for r_info in [0.0, 1.0]:float|missing, se in [0.0, 1.0]:float|missing, f(r_info, se) >= 0"
      basis: "probed, n=44"
    - step: "evidence"
      claim: "for r_info in [0.0, 1.0]:float|missing, se in [0.0, 1.0]:float|missing, f(r_info, se) <= r_info"
      basis: "probed, n=132"
    - step: "evidence"
      claim: "let g = arbital.plot._band_high, for r_info in [0.0, 1.0]:float|missing, se in [0.0, 1.0]:float|missing, f(r_info, se) <= g(r_info, se)"
      basis: "probed, n=132"
    - step: "evidence"
      claim: "let g = arbital.plot._band_high, for r_info in [0.0, 0.999]:float|missing, se in [0.0, 1.0]:float|missing, f(r_info, se) <= g(r_info, se)"
      basis: "probed, n=132"
    - step: "derivation"
      claim: ""
      basis: "no direct function dependencies, vacuously current"
  lineage:
    generated_by: "mathema 0.6.0"
    CDD_spec_version: "0.2.0"
    date: "2026-09-25"
    commit: "52a40a030d8c9056657d5c8dfc6b9b8e57d0789e"
  meta:
    mathema.premise_state: {}
    mathema.intent_provenance: "declared"
