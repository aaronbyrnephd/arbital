# machine record; binds to form cd9fe7e2d3b7
arbital.geometry._min_gap:
  schema_version: "0.2.0"
  name: "_min_gap"
  signature: "(p: 'int', min_gap_frac: 'float') -> 'float'"
  intent: "The smallest angular gap the \"spread\" layout will allow. A fraction of the even share 2*pi/p that p features would get."
  grammar: "mathema"
  tolerance: 1.0e-09
  identity:
    form: "cd9fe7e2d3b7"
    sig: "bca22a0914b4"
    tier: 2
    source_available: true
    pure: true
    claims_fingerprint: "b200d5b2a2df"
    pin: "none"
    integrity: "v2:e6aa44a37b59e55c"
  math:
    unicode: "2*π*min_gap_frac/p"
    latex: "\\frac{2 \\pi min_{gap frac}}{p}"
    srepr: "Mul(Integer(2), pi, Symbol('min_gap_frac', real=True), Pow(Symbol('p', real=True), Integer(-1)))"
  claims:
    - name: "at_the_full_fraction_it_is_the_even_share"
      statement: "for p in [1, 50]:int|missing, p*f(p, 1) = 2*pi"
      verdict: "proven"
      note: "bare 'pi' reads as the mathematical constant; inferred min_gap_frac=1 from the claim's own literal argument"
      sketch: "2*pi and 2*pi simplify identically"
      condition: "for p in [1, 50]:int|missing"
      route: "derive"
      authored:
        surface: "claims-file"
        route: "best"
      domain:
        p:
          base_type: "Z"
          pieces:
            - lo: 1.0
              hi: 50.0
              closed_lo: true
              closed_hi: true
          excluded: []
          explicit_type: true
      meta:
        mathema.float_companion: "at_the_full_fraction_it_is_the_even_share[float]"
        mathema.surface: "declared"
    - name: "at_the_full_fraction_it_is_the_even_share[float]"
      statement: "for p in [1, 50]:int|missing, p*f(p, 1) = 2*pi"
      verdict: "holds"
      n: 44
      note: "the implementation of at_the_full_fraction_it_is_the_even_share, executed in float at 44 points (every domain corner, then sampled interior points)"
      route: "probe"
      authored:
        surface: "claims-file"
      domain:
        p:
          base_type: "Z"
          pieces:
            - lo: 1.0
              hi: 50.0
              closed_lo: true
              closed_hi: true
          excluded: []
          explicit_type: true
      meta:
        mathema.surface: "declared"
        mathema.companion_of: "at_the_full_fraction_it_is_the_even_share"
        mathema.family: "is_numerically_stable"
    - name: "dependencies_current"
      verdict: "proven"
      note: "generated by mathema verify; adjudicated against the verified store"
      sketch: "no direct function dependencies, vacuously current"
      route: "derive"
      authored:
        surface: "builtin"
      meta:
        mathema.surface: "builtin"
    - name: "never_negative"
      statement: "for p in [1, 50]:int|missing, min_gap_frac in [0.0, 1.0]:float|missing, f(p, min_gap_frac) >= 0"
      verdict: "proven"
      sketch: "interval evaluation over the declared domain: 2*pi*min_gap_frac/p ∈ AccumBounds(0, 2*pi), never negative"
      condition: "where x=min_gap_frac: ∀ p ∈ [1, 50] ⊂ ℤ ∪ {∅}, x ∈ [0.0, 1.0] ⊂ ℝ ∪ {∅}"
      route: "derive"
      authored:
        surface: "claims-file"
        route: "best"
      domain:
        p:
          base_type: "Z"
          pieces:
            - lo: 1.0
              hi: 50.0
              closed_lo: true
              closed_hi: true
          excluded: []
          explicit_type: true
        min_gap_frac:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
      meta:
        mathema.float_companion: "never_negative[float]"
        mathema.surface: "declared"
    - name: "never_negative[float]"
      statement: "for p in [1, 50]:int|missing, min_gap_frac in [0.0, 1.0]:float|missing, f(p, min_gap_frac) >= 0"
      verdict: "holds"
      n: 44
      note: "the implementation of never_negative, executed in float at 44 points (every domain corner, then sampled interior points)"
      route: "probe"
      authored:
        surface: "claims-file"
      domain:
        p:
          base_type: "Z"
          pieces:
            - lo: 1.0
              hi: 50.0
              closed_lo: true
              closed_hi: true
          excluded: []
          explicit_type: true
        min_gap_frac:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
      meta:
        mathema.surface: "declared"
        mathema.companion_of: "never_negative"
        mathema.family: "is_numerically_stable"
    - name: "p_minimum_gaps_fit_in_one_turn"
      statement: "for p in [1, 50]:int|missing, min_gap_frac in [0.0, 1.0]:float|missing, p*f(p, min_gap_frac) <= 2*pi"
      verdict: "proven"
      note: "bare 'pi' reads as the mathematical constant"
      sketch: "interval evaluation over the declared domain: -2*pi*(min_gap_frac - 1) ∈ AccumBounds(0, 2*pi), never negative"
      condition: "where x=min_gap_frac: ∀ x ∈ [0.0, 1.0] ⊂ ℝ ∪ {∅}"
      route: "derive"
      authored:
        surface: "claims-file"
        route: "best"
      domain:
        p:
          base_type: "Z"
          pieces:
            - lo: 1.0
              hi: 50.0
              closed_lo: true
              closed_hi: true
          excluded: []
          explicit_type: true
        min_gap_frac:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
      meta:
        mathema.float_companion: "p_minimum_gaps_fit_in_one_turn[float]"
        mathema.surface: "declared"
    - name: "p_minimum_gaps_fit_in_one_turn[float]"
      statement: "for p in [1, 50]:int|missing, min_gap_frac in [0.0, 1.0]:float|missing, p*f(p, min_gap_frac) <= 2*pi"
      verdict: "holds"
      n: 44
      note: "the implementation of p_minimum_gaps_fit_in_one_turn, executed in float at 44 points (every domain corner, then sampled interior points)"
      route: "probe"
      authored:
        surface: "claims-file"
      domain:
        p:
          base_type: "Z"
          pieces:
            - lo: 1.0
              hi: 50.0
              closed_lo: true
              closed_hi: true
          excluded: []
          explicit_type: true
        min_gap_frac:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
      meta:
        mathema.surface: "declared"
        mathema.companion_of: "p_minimum_gaps_fit_in_one_turn"
        mathema.family: "is_numerically_stable"
    - name: "rises_with_the_fraction"
      statement: "for p in [1, 50]:int|missing, min_gap_frac in [0.0, 1.0]:float|missing, d(f(p, min_gap_frac), min_gap_frac) >= 0"
      verdict: "proven"
      sketch: "interval evaluation over the declared domain: 2*pi/p ∈ AccumBounds(pi/25, 2*pi), never negative"
      condition: "∀ p ∈ [1, 50] ⊂ ℤ ∪ {∅}"
      route: "derive"
      authored:
        surface: "claims-file"
        route: "best"
      domain:
        p:
          base_type: "Z"
          pieces:
            - lo: 1.0
              hi: 50.0
              closed_lo: true
              closed_hi: true
          excluded: []
          explicit_type: true
        min_gap_frac:
          lo: 0.0
          hi: 1.0
          closed_lo: true
          closed_hi: true
      meta:
        mathema.float_companion: "none (the claim has no point evaluation against the code)"
        mathema.surface: "declared"
  concepts:
    - "differentiation"
  references: {}
  reasoning:
    - step: "intent"
      claim: "The smallest angular gap the \"spread\" layout will allow. A fraction of the even share 2*pi/p that p features would get."
      basis: "documented; the author's stated purpose"
    - step: "formalization"
      claim: "2*π*min_gap_frac/p"
      basis: "symbolic lift of the body"
    - step: "derivation"
      claim: "for p in [1, 50]:int|missing, min_gap_frac in [0.0, 1.0]:float|missing, f(p, min_gap_frac) >= 0"
      basis: "interval evaluation over the declared domain: 2*pi*min_gap_frac/p ∈ AccumBounds(0, 2*pi), never negative"
    - step: "evidence"
      claim: "for p in [1, 50]:int|missing, min_gap_frac in [0.0, 1.0]:float|missing, f(p, min_gap_frac) >= 0"
      basis: "probed, n=44"
    - step: "derivation"
      claim: "for p in [1, 50]:int|missing, min_gap_frac in [0.0, 1.0]:float|missing, p*f(p, min_gap_frac) <= 2*pi"
      basis: "interval evaluation over the declared domain: -2*pi*(min_gap_frac - 1) ∈ AccumBounds(0, 2*pi), never negative"
    - step: "evidence"
      claim: "for p in [1, 50]:int|missing, min_gap_frac in [0.0, 1.0]:float|missing, p*f(p, min_gap_frac) <= 2*pi"
      basis: "probed, n=44"
    - step: "derivation"
      claim: "for p in [1, 50]:int|missing, p*f(p, 1) = 2*pi"
      basis: "2*pi and 2*pi simplify identically"
    - step: "evidence"
      claim: "for p in [1, 50]:int|missing, p*f(p, 1) = 2*pi"
      basis: "probed, n=44"
    - step: "derivation"
      claim: "for p in [1, 50]:int|missing, min_gap_frac in [0.0, 1.0]:float|missing, d(f(p, min_gap_frac), min_gap_frac) >= 0"
      basis: "interval evaluation over the declared domain: 2*pi/p ∈ AccumBounds(pi/25, 2*pi), never negative"
    - step: "derivation"
      claim: ""
      basis: "no direct function dependencies, vacuously current"
    - step: "situating"
      claim: "instantiates: differentiation"
      basis: "deterministic concept tagging"
  lineage:
    generated_by: "mathema 0.6.0"
    CDD_spec_version: "0.2.0"
    date: "2026-09-25"
    commit: "52a40a030d8c9056657d5c8dfc6b9b8e57d0789e"
  meta:
    mathema.concept_sources:
      mechanism:
        - "differentiation"
    concepts:
      - "differentiation"
    mathema.premise_state: {}
    mathema.intent_provenance: "declared"
