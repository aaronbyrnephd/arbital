# mathema index: codebase intent, modules, and function keys with source locations, regenerated by `mathema index`, not hand-edited
index:
  intent:
    text: "Make the first look at a dataset show every kind of association, not only the straight lines: each variable is drawn as an orbit around a target, so strength, shape, direction, redundancy and usefulness are readable from one figure."
    evidence: "explicit"
  modules: []
  lineage:
    generated_by: "mathema 0.6.0"
    CDD_spec_version: "0.1.0"
    date: "2026-09-16"
