# materialized declared layer; regenerated by mathema docsync; direct edits are read as an authoring surface on the next sync
arbital.geometry._widened_gap:
  claims:
    - name: "never_below_the_minimum"
      statement: "for gap in [0, 7], g_min in [0, 1], scale in [0, 1], f(gap, g_min, scale) >= g_min"
    - name: "keeps_the_order_of_the_gaps"
      statement: "for gap in [0, 7], g_min in [0, 1], scale in [0, 1], d(f(gap, g_min, scale), gap) >= 0"
    - name: "a_gap_already_at_the_minimum_is_unchanged"
      statement: "for g_min in [0, 1], scale in [0, 1], f(g_min, g_min, scale) == g_min"
  intent: "One angular gap after the \"spread\" rescaling. Every gap is raised to at least g_min, and whatever it had above g_min is kept in proportion `scale`, so the gaps that were large stay large relative to each other while the pile-ups are opened."
  grammar: "mathema"
