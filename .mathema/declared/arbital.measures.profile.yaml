# materialized declared layer; regenerated by mathema docsync; direct edits are read as an authoring surface on the next sync
arbital.measures.profile:
  claims:
    - name: "the_monotone_part_never_exceeds_the_total"
      statement: "assuming is_defined(f), f(x, y, 5, False, False)[\"r_mono\"] <= f(x, y, 5, False, False)[\"r_info\"]"
    - name: "the_nonlinear_share_is_a_share"
      statement: "assuming is_defined(f), 0 <= f(x, y, 5, False, False)[\"nonlinearity\"] <= 1"
  intent: "All association measures for one (x, y) pair, as a dict. Set x_discrete / y_discrete when a variable is categorical (nominal or an integer-coded label) so the correct MI estimator is used."
  grammar: "mathema"
