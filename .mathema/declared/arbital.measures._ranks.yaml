# materialized declared layer; regenerated by mathema docsync; direct edits are read as an authoring surface on the next sync
arbital.measures._ranks:
  claims:
    - name: "ranks_start_at_one"
      statement: "assuming is_defined(f), f(x)[0] >= 1"
  intent: "Ranks 1..n with ties given the average of the ranks they occupy."
  grammar: "mathema"
