# materialized declared layer; regenerated by mathema docsync; direct edits are read as an authoring surface on the next sync
arbital.plot._fallback_half_span:
  claims:
    - name: "stays_readable"
      statement: "for arc_length in [0, 2], r_display in [0.06, 1.06], 0.35 <= f(arc_length, r_display) <= 1.6"
    - name: "shrinks_at_a_larger_radius"
      statement: "for arc_length in [0, 2], r_display in [0.06, 1.06], d(f(arc_length, r_display), r_display) <= 0"
  intent: "Half-span of an arc when no association matrix was supplied. A fixed display length converted to an angle at that radius, kept inside a readable range."
  grammar: "mathema"
